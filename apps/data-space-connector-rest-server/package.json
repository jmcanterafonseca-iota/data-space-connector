{
	"name": "@twin.org/data-space-connector-rest-server",
	"version": "0.0.1-next.1",
	"description": "Data Space Connector REST Service",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/twinfoundation/data-space-connector.git",
		"directory": "apps/data-space-connector-rest-server"
	},
	"author": "jose.cantera@iota.org",
	"license": "Apache-2.0",
	"type": "module",
	"engines": {
		"node": ">=20.0.0"
	},
	"scripts": {
		"clean": "rimraf dist coverage",
		"build": "tsc",
		"dev": "concurrently \"tsc --watch\" \"nodemon\"",
		"merge-locales": "merge-locales",
		"test": "vitest --run --config ./vitest.config.ts --no-cache",
		"coverage": "vitest --run --coverage --config ./vitest.config.ts --no-cache",
		"docs:clean": "rimraf docs/open-api",
		"docs:api": "ts-to-openapi ./ts-to-openapi.json ./docs/open-api/spec.json",
		"docs": "npm run docs:clean && npm run docs:api",
		"dist": "npm run clean && npm run build && npm run merge-locales && npm run test && npm run docs",
		"dist:no-test": "npm run clean && npm run build && npm run merge-locales && npm run docs",
		"start": "node dist/es/index.js",
		"prepare": "ts-patch install -s"
	},
	"dependencies": {
		"@twin.org/background-task-models": "next",
		"@twin.org/core": "next",
		"@twin.org/data-space-connector-models": "0.0.1-next.1",
		"@twin.org/data-space-connector-service": "0.0.1-next.1",
		"@twin.org/engine": "next",
		"@twin.org/engine-types": "next",
		"@twin.org/engine-models": "next",
		"@twin.org/entity": "next",
		"@twin.org/nameof": "next",
		"@twin.org/node-core": "next"
	},
	"devDependencies": {
		"@twin.org/entity-storage-connector-memory": "next",
		"@twin.org/merge-locales": "next",
		"@twin.org/nameof-transformer": "next",
		"@twin.org/nameof-vitest-plugin": "next",
		"@twin.org/ts-to-openapi": "next",
		"@types/node": "24.3.0",
		"@vitest/coverage-v8": "3.2.4",
		"concurrently": "9.2.0",
		"copyfiles": "2.4.1",
		"cross-env": "10.0.0",
		"nodemon": "3.1.10",
		"rimraf": "6.0.1",
		"ts-patch": "3.3.0",
		"typedoc": "0.28.10",
		"typedoc-plugin-markdown": "4.8.1",
		"typescript": "5.9.2",
		"vitest": "3.2.4"
	}
}
